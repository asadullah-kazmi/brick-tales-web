/**
 * Service layer configuration.
 * Set USE_MOCK_API to false and implement real API calls in each service
 * when the backend is ready.
 */
export const USE_MOCK_API =
  process.env.NEXT_PUBLIC_USE_MOCK_API !== "false";
